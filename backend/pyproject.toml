[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "flask==3.1.3",
    "flask-cors>=6.0.5",
    "flask-limiter>=4.1.1",
    "gunicorn>=26.1.0",
    "pydantic>=2.13.4",
    "pytest==9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-playwright>=0.9.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.16.4",
    "ty>=0.0.73",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["app", "tests"]
pythonVersion = "3.14"
